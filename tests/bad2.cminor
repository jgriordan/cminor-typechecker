// test some incorrect uses of operators
// the amount of error in these couple lines makes me laugh (or maybe that is the sleep deprivation)

a: integer;
f: function integer(b:integer) = {
	a = b + 'c';
	c: boolean = -false && "false" < true;
	b = !(a ^ c);
	return 1 + 2 - 3 * 4 / 5 % 6 ^ a;
}
